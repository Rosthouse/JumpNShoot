///Resetgame
{
    room_goto(rm_Reset);

    /*///Reset to start Position
    with(obj_Player){
        x = xstart;
        y = ystart;

        vspeed = 0;
    }


    //Reanimating all enemies
    instance_activate_object(par_Enemy);
    
    //Destroying all bullets that are still flying around
    with(par_Bullet){
        instance_destroy();
    }*/
}
